export type recive_mqtt = {
  obj_name: {
    name: string;
    floor: string;
    status: string;
  };
};

export type parking_data = {
  name: string;
  floor: string;
  status: string;
};

export type reservation_data = {
  id: string;
};
